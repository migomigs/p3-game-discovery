const SearchBar = () => {
  return (
    <div className="form-control w-full">
      <input
        type="text"
        placeholder="Search"
        className="input input-bordered"
      />
    </div>
  );
};

export default SearchBar;
